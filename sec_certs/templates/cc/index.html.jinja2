{% extends "base.html.jinja2" %}
{% from "cc/utils.html.jinja2" import render_pagination %}
{% block content %}
	<h1>Common Criteria</h1>
	<p class="lead">This page provides a listing of Common Criteria certificates.</p>

    <a href="{{ url_for(".network") }}" class="btn btn-primary">Graph</a>
    <a href="{{ url_for(".search") }}" class="btn btn-primary">Search</a>

    {{ render_pagination(pagination, certs) }}
{% endblock %}