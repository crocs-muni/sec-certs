<div class="card">
    <div class="card-body">
        <h5 class="card-title">Certificate reappeared</h5>
        <p class="card-text">A Common Criteria certificate for a product named <b>{{ cert["name"] }}</b>
            is available again.</p>
        {% if linkback %}
            <a href="{{ url_for("cc.entry", hashid=hashid) }}" class="card-link">Certificate page</a>
        {% endif %}
    </div>
</div>