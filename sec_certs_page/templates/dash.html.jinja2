{% extends "common/base.html.jinja2" %}
{% block head %}
    {# TODO: This template is not fully correct. The metas are duplicated. #}
    {{ super() }}
    {{ metas|safe }}
    {{ favicon|safe }}
    {{ css|safe }}
{% endblock %}
{% block content %}
    {{ app_entry|safe }}
{% endblock %}
{% block footer %}
    {{ super() }}
    {{ config|safe }}
    {{ scripts|safe }}
    {{ renderer|safe }}
{% endblock %}
