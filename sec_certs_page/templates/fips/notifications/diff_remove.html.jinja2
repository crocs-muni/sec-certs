<div class="card">
    <div class="card-body">
        <h5 class="card-title">Certificate removed</h5>
        <p class="card-text">A FIPS 140 certificate for a product named <b>{{ cert["name"] }}</b>
            is not available. This means that either its certification report or security target
            could not be downloaded during processing.</p>
        {% if linkback %}
            <a href="{{ url_for("fips.entry", hashid=hashid) }}" class="card-link">Certificate page</a>
        {% endif %}
    </div>
</div>